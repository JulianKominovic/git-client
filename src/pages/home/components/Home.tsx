import React, { useEffect } from "react";
impo;

type Props = {};

const Home = (props: Props) => {
  useEffect(() => {}, []);

  return (
    <div style={{ height: "100vh" }}>
      <div id="graph"></div>
    </div>
  );
};

export default Home;
